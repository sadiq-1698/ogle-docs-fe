import "../../styles/globals.css";

export const metadata = {
  title: "Document | Ogle Docs",
  description: "Your document",
};

export default function DocsLayout({ children }) {
  return <div>{children}</div>;
}
